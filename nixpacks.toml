[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python projectsite/manage.py collectstatic --no-input"]

[start]
cmd = "daphne -b 0.0.0.0 -p $PORT projectsite.asgi:application"
