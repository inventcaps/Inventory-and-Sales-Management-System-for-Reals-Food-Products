{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && python projectsite/manage.py collectstatic --no-input"
  },
  "deploy": {
    "startCommand": "daphne -b 0.0.0.0 -p $PORT projectsite.asgi:application",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
